# Application Port (Automatically provided by most Node.js cloud hosts like AiroApp / cPanel / Render / Railway)
PORT=8080

# Environment Mode
NODE_ENV=production

# Hosted MySQL Database Configuration (Automatically injected by AiroApp runtime environment)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=workshop_manager
DB_USER=root
DB_PASSWORD=
